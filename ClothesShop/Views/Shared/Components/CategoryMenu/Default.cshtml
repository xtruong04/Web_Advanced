@model IEnumerable<ClothesShop.Models.Category>

@foreach (var item in Model)
{
    <a asp-controller="Shop"
       asp-action="Index"
       asp-route-cateId="@item.Id"
       class="dropdown-item">
        @item.Name
    </a>
}